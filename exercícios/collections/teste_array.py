import array as arr

# cria uma instância do array, do tipo ponto flutuante e informando os valores
# O array em python é uma colecão de valores do mesmo tipo e portanto, é criado na memória de forma otimizada
arr.array('d', [1, 3.5])
print(arr[1])